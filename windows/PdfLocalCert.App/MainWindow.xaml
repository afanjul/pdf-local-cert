<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PdfLocalCert.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PDF Local Cert">

    <Grid RowDefinitions="Auto,*" Padding="24" RowSpacing="16">
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="PDF Local Cert" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Windows shell (scaffold)" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="12" VerticalAlignment="Top">
            <Button x:Name="PingButton" Content="Ping core" Click="OnPingClicked" />
            <TextBlock x:Name="StatusText" TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </Grid>
</Window>
